server:
  port: 8081

logging:
  level:
    org.springframework: DEBUG

spring:
  application:
    name: echo-preauthorized
  security:
    oauth2:
      resourceserver:
        jwt:
#          issuer-uri: http://127.0.0.1:8080
          jwk-set-uri: http://localhost:8080/oauth2/jwks

